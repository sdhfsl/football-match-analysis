---
name: football-match-analysis
description: >
  足球赛前深度分析：从真实数据源抓取盘口、伤停、交锋、技术统计与出线形势，输出不编造数字的结构化赛前报告。
  覆盖竞彩让球、亚盘、欧赔、大小球四类盘口的区分与判读，以及八强/晋级路径和动机不对称分析。
  Use this skill whenever the user asks for 足球赛前分析、比赛分析、竞彩分析、亚盘分析、盘口分析、
  赛前报告、预测比分、让球判断、出线形势，或提到某一场具体比赛（例如"帮我算明天某队 vs 某队"）、
  竞彩编号、亚盘水位、大小球。也适用于单纯抓取足球数据（盘口、伤停、交锋记录、技术统计）。
  Triggers: "赛前分析", "足球分析", "竞彩", "让球", "亚盘", "欧赔", "大小球", "盘口", "水位",
  "预测比分", "比赛预测", "出线形势", "帮我算一场球", "分析这场比赛", "算明天那场",
  "football match analysis", "pre-match analysis", "asian handicap", "handicap", "predict score".
  即使用户只给了对阵和竞彩编号、没有明说"分析"，也应使用本 skill。
---

# 足球赛前深度分析

目标：输出一份**每个数字都能溯源**的赛前报告。做不到的部分明确标注，而不是用听起来合理的数字填满表格。

## 三条铁律

**1. 不编数字。** 这是本 skill 存在的理由。数据缺失时留空并标注，且必须区分两种情况：

- **源被拦**（Cloudflare 403、验证码、域名不可达）——说明是哪个源、什么原因，换源或换技术重试。
- **数据不存在**（该赛事没有公开 xG、赛前没有预计首发）——直接说明"不存在"，不要暗示"没抓到"。

绝不允许为了让表格好看而推测填充。一份"半边实、半边空"的报告对用户有用；一份全填满但一半是编的报告会让人输钱。

**2. 盘口必须分类，不能混。** 同一个"让 1 球"在两种盘口下完全不同：

| 类型 | 示例 | 判读 |
|---|---|---|
| 竞彩让球（整数盘） | 主队让 1 球 | 赢 2 球为让胜，**赢 1 球为让平**，不胜为让负。没有走盘 |
| 亚洲盘（含半球/四分之一球） | 主队 -1.25 | **赢 1 球算输半**，赢 2 球才全赢 |
| 欧赔 | 2.02 / 2.90 / 3.45 | 判断市场胜平负倾向 |
| 大小球 | 大 2.25 / 小 2.25 | 判断进球预期 |

竞彩让 1 与亚盘 -0.5 / -1.25 之间的落差本身就是重要信号，见下"背离检查"。

**3. 先找结构性拐点，再谈实力。** 实力对比通常已经反映在赔率里，真正有价值的是赔率不一定充分反映的东西：出线形势、必须赢 vs 可接受平局的动机不对称、赢下这场的淘汰赛对手是谁。**先做这一步，再套模板。**

实例（2026 亚运会男足 D 组末轮）：韩国 vs 沙特，两队均已提前出线。表面是"走过场"，但赢球＝八强打越南、不赢＝八强打三战全胜的乌兹别克斯坦。小组第一价值极大，双方都派最强阵容真打。这个事实把"低强度平局"的假设整个推翻，最终 2-0。

## 工作流

### 阶段 0：锁定比赛

用户说"明天那场"这类模糊指代时，**先列出候选再选**。中文用户提到"明天"通常指北京时间次日。竞彩编号是最可靠的锚点。

同一天有多场时，默认选竞彩编号最靠前的那场（通常是主推场次），并在回答开头说明你选了哪场、另一场是什么，让用户一句话就能纠正。

### 阶段 1：抓数据

读 `references/sources.md` 获取完整的源地图、可用端点、以及每个源的访问方式。要点：

- **盘口（竞彩）** → 500.com 竞彩列表页（`curl` 直取）
- **盘口（亚盘/大小球/欧赔平均值）+ 情报 + 伤停** → 雷速体育，**必须用浏览器**（见下）
- **技术统计（控球/射门/角球/牌数）** → 雷速比赛页，**必须用 evaluate 读 DOM**（见下）
- **小组积分/赛果/大名单/射手榜/淘汰赛对阵** → Wikipedia，表格要抓 HTML 解析，不能用 `prop=extracts`
- **天气** → `wttr.in/{城市}`

### 阶段 2：交叉验证

关键事实至少两个独立源。最值得验证的是**比赛时间**——时区换算最容易出错。

实例：韩国 vs 沙特的时间通过三个源确认——竞彩页 18:00（北京）、Wikipedia 19:00 JST、TheSportsDB 10:00 UTC，三者一致（JST=UTC+9，北京=UTC+8）。

赔率数据的自洽性也要校验。**让球盘的隐含概率必须与非让球盘一致**：

```
让胜 + 让平 ≈ 非让球盘的主胜概率
```

若不吻合，说明赔率读取（字段顺序/盘口方向）错了，回到源头重读，不要带着错数据往下做。

### 阶段 3：找结构性拐点

固定要问的问题：

1. **双方动机是否对称？** 谁必须赢（输不起）、谁可以接受平局？平局分别对各队意味着什么？
2. **赢下这场之后对阵谁？** 淘汰赛/下一轮对手的强弱会改变"争第一"的价值。
3. **同组其他比赛的结果什么时候出？** 如果比本场早开球，它会在开赛前改变双方的动机——这是一个**有明确解出时间的不确定性**，要在报告里点名。
4. **赛程与休整是否对等？** 休整天数差、是否轮空、场地是否陌生（双方是否都没在这块场地踢过）。
5. **减员是否对称？** 谁停赛（红牌累计）、谁带伤、谁是核心。

### 阶段 4：出报告

按 `references/report-template.md` 输出。必含：数据来源与可信度声明、四模块分析、最终结论汇总表、**不可得清单**。

### 阶段 5：归档盘口（下次才有初盘）

**初盘无法从公开源回溯获取**——实测结论：雷速的数据结构里只有即时值，没有初盘字段；500.com 的盘口详情页有交互式验证码。唯一可靠的办法是**自己存基线**。

每次取到盘口，把数值连同时间戳写进报告文件。同一场比赛第二次取数时，两次的差值就是水位/赔率移动方向。

```markdown
| 盘口 | 时间戳 | 主水/盘口/客水 |
|---|---|---|
| 亚盘平均 | 2026-09-22 19:46 | 1.02 / 0.5 / 0.77 |
| 亚盘平均 | 2026-09-22 20:05 | 1.02 / 0.5 / 0.77（未变） |
```

## 关键技法

### 被 WAF 保护的站点用真浏览器，不要逆向

阿里云 WAF 的 `acw_sc__v2` cookie 挑战、各种 JS cookie 校验：**正解是用浏览器打开页面**（见 `browser-use:control-browser` skill），浏览器会自己执行 JS、自己设 cookie。curl 拿到的是一个 20KB 的空壳挑战页，这是识别特征。

**但交互式验证码（人类勾选框）不要绕。** 那是站点明确设置的人机验证，属于边界。腾讯 EdgeOne 的 "Security Verification" 页面就是这类——遇到就换源，不要试图破解。

### 图表渲染的数据用 evaluate 读 DOM

雷速的技术统计（控球率、射门、角球、牌数）是图表组件，**无障碍树的 domSnapshot 里看不到数值**，但数据在 DOM 里。用 `playwright.evaluate` 直接读：

```js
const stats = await tab.playwright.evaluate(() => {
  const rows = [];
  document.querySelectorAll(".bar-panel").forEach((p) => {
    const c = p.querySelector(".barcenter");
    if (!c) return;
    rows.push({
      stat: c.textContent.trim(),
      home: (p.querySelector(".left .tnum") || {}).textContent,
      away: (p.querySelector(".right .tnum") || {}).textContent,
    });
  });
  // 角球/黄牌/红牌在 .ts-top，顺序: 主角球, 主红, 主黄, 客黄, 客红, 客角球
  const top = document.querySelector(".ts-top");
  const labs = top
    ? Array.from(top.querySelectorAll(".lab")).map((e) => e.textContent.trim())
    : [];
  return { rows, labs };
});
```

**通用原则：domSnapshot 看不到的东西，先怀疑是图表/canvas/SVG 渲染，再试 evaluate 读 DOM 或读内嵌 JSON。不要因为无障碍树没有就判定"取不到"。**

### 一次调用里批量取多场比赛

浏览器调用较贵，导航可以在同一个 JS cell 里串起来。先从列表页抓出所有目标 URL，再循环导航 + evaluate：

```js
const links = await tab.playwright.evaluate(() => {
  const seen = new Set(), out = [];
  document.querySelectorAll("a[href*='detail-']").forEach((a) => {
    const href = a.getAttribute("href");
    if (seen.has(href)) return;
    seen.add(href);
    const row = a.closest("tr");
    out.push({ href, txt: row ? row.innerText.replace(/\s+/g, " ").trim().slice(0, 70) : "" });
  });
  return out.filter((o) => /亚运男足/.test(o.txt)).slice(0, 6);
});

const results = [];
for (const l of links) {
  await tab.goto(l.href);
  await tab.playwright.waitForLoadState({ state: "domcontentloaded" });
  await tab.playwright.waitForTimeout(1800);
  results.push({ match: l.txt, ...(await tab.playwright.evaluate(/* 上面的提取器 */)) });
}
```

### Wikipedia 的表格拿不到要换方式

`action=query&prop=extracts&explaintext=1` 会**剥掉所有表格**，只留正文。积分榜、赛果、大名单全在表格里，必须抓 HTML 再用 BeautifulSoup 解析：

```python
soup = BeautifulSoup(html, "lxml")
for s in soup(["sup", "style", "script"]):
    s.decompose()          # 去掉脚注引用，否则数字会粘连
tables = soup.find_all("table")
# 小组积分表按 section 标题定位：t.find_previous("h3").get_text()
```

大名单通常在独立子页面（`...– Men's team squads`），主页面里只有链接。

## 数据可得性矩阵

实测（2026-09，亚运会男足赛事）：

| 数据 | 可得性 | 来源 / 原因 |
|---|---|---|
| 竞彩让球数、胜平负、让球胜平负、开售项 | ✅ | 500.com 竞彩列表页 |
| 亚盘 / 大小球 / 欧赔 **平均值** | ✅ | 雷速情报页（需浏览器） |
| 伤停 / 停赛 | ✅ | 雷速情报页；**可能明确回复"暂无伤停情况"，这是有效答复** |
| 停赛（红牌） | ✅ | 雷速技术统计的红牌数可交叉验证媒体报道 |
| 历史交锋（含赛事类型） | ✅ | 雷速数据分析页 |
| 技术统计：控球/射门(射正)/进攻/危险进攻/角球/黄牌/红牌 | ✅ | 雷速比赛页 + evaluate 读 DOM |
| 小组积分、赛果、大名单、射手榜、淘汰赛对阵 | ✅ | Wikipedia（表格需解析 HTML） |
| 赛程时间（交叉验证用） | ✅ | TheSportsDB 免费 key |
| 出线形势与淘汰赛路径 | ✅ | 由积分表推导，需自己算 |
| 天气 | ✅ | wttr.in |
| **初盘 / 水位移动** | ❌ | 雷速数据结构只有即时值；500.com 详情页有交互式验证码 → **只能自己归档** |
| **初赔 → 即时赔变化** | ❌ | 同上，同一根因 |
| **预计首发** | ❌ | 该级别赛前不存在；首发要等开赛前约 1 小时官方公布 |
| **xG / xGA** | ❌ | 此级别赛事无公开 xG（**数据不存在**，非取数失败）。用 射门(射正) 当代理指标 |
| 角球趋势 / 牌数趋势 | ⚠️ | 需逐场累积各场比赛的技术统计后自行计算 |

## 分析要点

### 背离检查（竞彩 vs 亚盘）

竞彩让球是整数盘，亚盘可能是半球/四分之一球。**两者的落差本身是信号。**

实例：中国 vs 阿联酋，竞彩要求**中国赢 2 球**才算让胜（4.50），而亚盘平均只给**中国 -0.5**、水位还偏向阿联酋受让（0.77）。差了一整个球。校验方式：亚盘 -0.5 之下中国必须赢才全赢，而中国胜率仅 43.8%，所以中国水位本该高于 1.00——实测 1.02，与欧赔隐含概率吻合，**说明这不是数据错误，而是市场的真实态度**。

### 隐含概率要自己去水

```python
raw = [1/o for o in odds]           # 1/2.02, 1/2.90, 1/3.45
total = sum(raw)                     # ≈ 1.13（水位）
probs = [r/total*100 for r in raw]   # 去水后的概率
```

然后用一致性校验确认读数正确（见阶段 2）。

### 用比赛方式判断盘口是否现实

不要只看实力和动机，要看**球队的比赛方式支不支持这个盘口**。

实例：中国两场小组赛控球率 32% 和 31%，场均 2 个角球、7.5 次射门。**一支场均控球 31% 的球队，要求它净胜 2 球是违背其打法**——这是"回避让胜"最硬的依据，比"动机上不需要赢两球"强得多。

技术统计正是为此存在：它把"强弱"变成"打法"，而打法才决定净胜球。

### 置信度要敢给低

当主胜与平局的隐含概率接近（如 43.8% vs 30.5%），或结果高度依赖另一个未出结果的变量时，**置信度给"低"是正确的，不是保守**。报告的价值在于把不确定性说清楚，而不是给出一个自信的假象。

## 输出与交付

报告写到 `reports/YYYY-MM-DD-{home}-vs-{away}.md`（相对当前工作目录）。文件名用日期+对阵，便于后来者按日期检索与复用。

## 边界

- **不绕交互式验证码。** JS cookie 挑战用浏览器是正常使用；人类勾选框是站点明确的人机验证，遇到就换源。
- 报告只用于赛事研究与数据讨论，**不构成投注建议**，这一声明必须保留。
- 遇到与用户预期不符的数据（例如某队战绩远好于/差于印象），**照实呈现并给出解释**（对手质量、样本大小），不要为了符合预期而调整数据。
